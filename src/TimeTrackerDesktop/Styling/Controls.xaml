<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Shared control styles (plan Task 4.2).

         UseSystemFocusVisuals is not decoration: the plan requires visible keyboard focus, and the default
         focus visual on a borderless, always-on-top card is easy to lose against the card's own background. -->
    <Style x:Key="TrackingIconButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="44" />
        <Setter Property="Height" Value="44" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="UseSystemFocusVisuals" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="WidgetMutedTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{ThemeResource WidgetMutedBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Style" Value="{StaticResource CaptionTextBlockStyle}" />
    </Style>
</ResourceDictionary>
